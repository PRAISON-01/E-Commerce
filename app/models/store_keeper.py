# from dataclasses import Field
# from pydantic import BaseModel, EmailStr
#
#
# class RegisterStoreKeeper(BaseModel):
#     name : str = Field(..., min_length=3, max_length=100)
#     password = Field(..., min_length=3, max_length=100)
#     email : EmailStr
#
#
#
